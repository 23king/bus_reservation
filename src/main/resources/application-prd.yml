server:
  port: 8080

spring:
  profiles:
    active: prd
  datasource:
    url: jdbc:log4jdbc:mysql://localhost:3306/reservation?useUnicode=true
    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DriverSpy
    username: sansu
    password: "!bus123@"
    initialize: false
    platform: all
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create
#      ddl-auto: none
      debug: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: OFF
    jdbc: OFF
    jdbc.sqltiming: DEBUG
    jdbc.resultsettable: DEBUG